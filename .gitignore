# Rust
/target/
**/*.rs.bk
*.pdb

# Logs
*.log

# mise
mise.local.toml
.mise.local.toml

# Project specific temporary files
test_write.md
new-features.md

# Toolchain / Cache
undefined/

# Note: Cargo.lock should typically be tracked in version control for projects that contain binary crates (CLI, GUI, etc.) to ensure reproducible builds.
