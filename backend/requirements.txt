# ── Core ──────────────────────────────────────────────────────────────────────
typer==0.25.1
rich==15.0.0
loguru==0.7.3
psutil==7.2.2
pyyaml==6.0.3
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.1

# ── Dev / Linting ─────────────────────────────────────────────────────────────
ruff==0.15.14
pre-commit==4.6.0
pytest==8.3.5

# ── Forensics ─────────────────────────────────────────────────────────────────
pytsk3>=20260418
volatility3==2.7.0
cryptography>=44.0.0

# ── Mobile ────────────────────────────────────────────────────────────────────
adb-shell==0.4.4

# ── Optional (require MSVC Build Tools / native libs on Windows) ──────────────
# Install MSVC Build Tools first: https://visualstudio.microsoft.com/visual-cpp-build-tools/
# Then: pip install yara-python pymobiledevice3 pyewf
#
# yara-python==4.5.1        — YARA rule scanning (needs C compiler)
# pymobiledevice3==4.14.16  — iOS acquisition (deps pylzss/lzfse need C compiler)
# pyewf                     — E01 image support (needs libewf-dev)

# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.109.2
uvicorn==0.27.1

# ── GUI ───────────────────────────────────────────────────────────────────────
customtkinter>=5.2.0
textual
